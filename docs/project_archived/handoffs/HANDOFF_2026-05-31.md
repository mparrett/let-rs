# Session Handoff

**Created:** 2026-05-31T23:40:46-0700
**Session ID:** 805a9b5b-368e-4563-9b74-c18f61032995
**Working Directory:** /Users/matt/projects-new/3p/letrs

## What to read first

The user paused intentionally after the ADR-022 draft landed — they were feeling the burn from the scope (30+ signatures + 15 tests for structured parse errors). ADR-022 is a **design draft only**, not in-progress implementation. The next session should not assume the user wants to immediately start implementing it. Re-confirm direction before touching code.

## Summary

Long session — workspace-wide hygiene sweep, the Elder Futhark rune refactor, the `letrs → let-rs` project rename, audit + codex-review triage with both archived, ADR-020 shipped (prims to globals; `(define +)` overwrites), ADR-021 shipped (letrec Rc cycle pinned with a diagnostic test, fix deferred), and ADR-022 drafted (structured parse errors with source spans, not implemented). 121 tests pass; wasm builds clean at 183K.

## Current State

Branch `main`, clean worktree, fully pushed to `origin/main` (GitHub repo renamed to `mparrett/let-rs` mid-session; remote URL updated). Today's commits, oldest first:

```
278a543  runes: take resolve as &[Tok] instead of by-value
4b7726f  docs: drop test-count line from CLAUDE.md
e881b7f  docs: note preferred web-serve commands in key_facts
f6e92cb  web: relocate letrs.html into web/ and link it from index
b08b7fa  web: move ⌫ button to right of tape/axiom input
e726db1  web: call letrs.html what it is — the dev log
45bbf4c  runes: differentiate spell glyph set by Elder Futhark meaning
0d42f5b  web: restyle dev-log link to match masthead voice
af6e4ff  project: rename letrs → let-rs
d915194  docs: triage 2026-05-29 audit; file two open items, archive the rest
7656fdd  docs: ADR-020 draft — prims to globals; (define +) overwrites
52a1b17  lisp: implement ADR-020 — prims live in globals; (define +) overwrites
023f348  docs: triage 2026-05-26 codex reviews; archive both
d078c59  docs+lisp: ADR-021 — letrec Rc cycle, pinned + deferred
1059b68  docs: ADR-022 draft — structured parse errors with source spans
```

Pre-session baseline was at `a3ae615` (web: hold cast visible for 2s).

## Uncommitted State / Untouched

- *Uncommitted:* none. Worktree is clean.
- *Untouched (deliberate):*
  - Local repo directory `/Users/matt/projects-new/3p/letrs` — user said they'd rename to `let-rs` themselves later. Don't auto-rename.
  - GitHub Pages workflow was drafted at `.github/workflows/pages.yml` during the session, then deleted (never committed) after the user reconsidered going public. Pages source in repo settings is set to "GitHub Actions" with no workflow present (dormant). Don't restore the workflow without checking.
  - `docs/project_incoming/issue_3.md` — deferred at the top (world snapshots), waiting on a concrete consumer. Don't act on it.
  - Decisions.md historical ADR entries (e.g. ADR-015 "Move prims to globals" deferred bullet) were updated to `~~strike~~` + DONE marker when ADR-020 landed; subsequent audits' historical references inside `docs/project_archived/` were intentionally left as time-capsule artifacts.

## In Progress

Nothing strictly mid-implementation. ADR-022 is on disk as a design draft; no Phase 1 code has been written. The user explicitly chose to pause here rather than continue.

## Gotchas

- **The auto-mode classifier blocks raw `python3 -m http.server`** — use `just wasm-serve` or `npx serve web` instead. Recorded in `docs/project_notes/key_facts.md` (WASM toolchain section).
- **Browser caching bit us once** — when the user said "I don't see it" after the rune refactor, they were viewing a cached page over the Tailscale URL `http://100.126.31.103:6987/`. If they report missing changes, ask them to hard-refresh and curl-verify the served bytes before chasing layout bugs.
- **Don't make unprompted code tweaks in response to "I don't see it"** — the cached-view confusion led to a brief design-review of CSS that wasn't actually needed. Diagnose first.
- **`(define + 5)` is now a loud footgun** post ADR-020 — it overwrites the prim slot; the next call errors with `not callable: 5`. Pre-ADR-020 it was silently inert. Worth knowing if a user reports `+` "broken" in the REPL.
- **`letrec_cycle_persists_after_drop` (tests/eval.rs)** asserts the cycle leak EXISTS today. If you implement a letrec fix later (Y-style desugaring, closure conversion, CESK Store), the assertion flips to `is_none()` and the test needs an update or removal at that point. The test comment documents this.
- **Test counts are no longer maintained in CLAUDE.md or let-rs.html** — deliberately dropped because they drift. Don't add new claims like "121 tests pass".
- **`docs/letrs.html` was renamed to `web/let-rs.html`** during the session. Historical references in `docs/project_incoming/` and `docs/project_archived/` still say `docs/letrs.html` — those are archival, don't update them.
- **The masthead is `Let-<span class="sigil">rs</span>`** — verified via Playwright screenshot. The hyphen stays attached to the plain "Let" so the italic indigo "rs" keeps its sigil weight. If the user wants to retune typography, the lever is `letter-spacing` or `margin-left` on `.sigil`, not the markup.

## Next Steps

The user signaled fatigue; don't assume forward motion. When the next session opens, ask what shape of work fits their energy. Options ranked by remaining value, all genuinely deferrable:

1. **ADR-022 Phase 1 implementation** — the design is on disk in `decisions.md:1654`. Add `crates/lisp/src/error.rs` (`LispErr` + `Span` + `Display` + `From<String>`/`From<&str>`), span the tokenizer, plumb through `read_datum` / `compile`, flip `eval_str`'s public signature, update ~15 string-match test assertions, add the three pinned tests from the ADR's implementation order section. Honest estimate: full day, not half. Step 1-8 ordered in the ADR.
2. **issue_4 (REPL + labs share Vm)** — `docs/project_incoming/issue_4.md`. P3 UX, niche. Probably 1-3 hours. Two viable shapes: split into two Vms (REPL gets its own globals; labs unaffected) or add a `vm.reset_globals_to_prelude()` affordance hosts can call.
3. **CESK upgrade** — meaty, deferred until a UX (undo button, replay scrubber) pulls. Don't start unprompted.
4. **`letrec` fix** — deferred per ADR-021 until either a host observes growth or the CESK upgrade lands.
5. **Persistent maps, hygienic macros, the play loop** — all in let-rs.html's "What comes after"; none have a consumer pulling.

If the user explicitly wants forward motion but small-ish: option 2 (issue_4) is the cleanest small win remaining.

## Open Tickets

`docs/project_incoming/` currently holds two tickets, neither currently `status: in_progress`:

- `issue_3.md` — **deferred** at the top (world snapshots; waiting on a concrete consumer)
- `issue_4.md` — open P3 (REPL+labs share VM clobber risk)
