[package]
name    = "curves"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
lisp = { path = "../lisp" }

# Dev-only: cast tests round-trip stroke tapes through the engine to
# verify rewrite + draw + render together. `strokes` is a test dep
# only — the crate itself doesn't need to know about the tape format.
[dev-dependencies]
strokes = { path = "../strokes" }
